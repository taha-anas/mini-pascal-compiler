program operations;
var num1, num2, result,;

begin
  read(num1);
  read(num2);

  result := num1 + num2;
  write(result);

  result := num1 - num2;
  write(result);

  result := num1 * num2;
  write(result);

  result := num1 / num2;
  write(result);

  result := num1 + (num2*num1);
  write(result);

end.
